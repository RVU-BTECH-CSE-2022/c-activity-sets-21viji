#include <stdio.h>
int main()
{  
  int a,b,add;
  add=0;
  printf("enter the first  numbers");
  printf("enter the second number");
  scanf("%d,%d",&a,&b);
  add=a+b;
  printf("summation of two numbers is %d",add);
  return 0;
}